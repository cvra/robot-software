sudo: false
dist: bionic
addons:
    apt:
        packages:
            - python
            - python3-dev
            - python-pip
            - python-virtualenv
            - protobuf-compiler

language: cpp

matrix:
    include:
        - compiler: gcc
          env: BUILD_TYPE="tests"
        - compiler: gcc
          env: BUILD_TYPE="build" PLATFORM="uwb-beacon"

before_install:
    - git submodule update --init --recursive

install:
    - ci/install.sh

script:
    - ci/build.sh
