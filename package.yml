dependency-dir: ./src/

target.arm:
    - src/main.c
    - src/blocking_uart_driver.c
    - src/control.c
    - src/encoder.c
    - src/motor_pwm.c
    - src/analog.c
    - src/cmp/cmp.c

depends:
    - pid
    - serial-datagram
    - cmp_mem_access
    - filter:
        fork: froj

#tests:

templates:
    Makefile.include.jinja: src/src.mk
